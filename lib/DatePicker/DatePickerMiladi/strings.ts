import { miladi } from '@lib/shared/strings';
import { DatePickerStrings } from '../shared/types';

const strings: DatePickerStrings = {
  en: {
    ...miladi.en,
    action: 'Apply',
  },
  fa: {
    ...miladi.fa,
    action: 'اعمال تاریخ',
  },
};

export default strings;
